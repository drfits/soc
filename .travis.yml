language: java
jdk: oraclejdk8
sudo: false # faster builds
branches:
  only:
    - master
    - develop
script:
  - "mvn clean install"
  - "mvn test"
  - "mvn cobertura:cobertura coveralls:report -DrepoToken=$COVERALLS_TOKEN"
cache:
  directories:
  - "$HOME/.m2"

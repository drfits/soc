language: java
jdk: oraclejdk8
sudo: false # faster builds
branches:
  only:
    - master
    - develop
script:
  - "mvn clean install cobertura:cobertura coveralls:report -DrepoToken=$COVERALLS_TOKEN"
cache:
  directories:
  - "$HOME/.m2"
